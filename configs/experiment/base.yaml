env:
  num_agents: 6
  dim: 2
  batch_size: 4
  max_steps: 50
  dynamic: true

algo:
  lr_actor: 3e-4
  lr_critic: 1e-3
  gamma: 0.99
  gae_lambda: 0.95
  clip_eps: 0.2
  ppo_epochs: 4
  minibatch_size: 128
  entropy_coef: 0.001
  max_steps_per_episode: 20
  num_episodes: 50
